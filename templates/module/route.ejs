import express from 'express';

export default ({ <%= camelName %>Controller }) => {
  const router = express.Router();
  router.get('/:id', <%= camelName %>Controller.findById.bind(<%= camelName %>Controller));
  return router;
};
