export default class <%= useCaseClassName %> {
  constructor({ <%= camelName %>Repository }) {
    this.<%= camelName %>Repository = <%= camelName %>Repository;
  }

  async execute(data) {
    // Implement use case logic
    return this.<%= camelName %>Repository.findById(data.id);
  }
}
